interface IPackage {
    id: Number,
    name: String,
    author: String,
    description: String,
    likeNumber: Number,
    warningNumber: Number,
    download: Number,
    img: string,
    version: String,
    mode: String,
    slug: String,
    imgDetail: ISlider[]
}