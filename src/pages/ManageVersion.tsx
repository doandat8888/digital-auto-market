import { useState } from "react";

const ManageVersion = () => {

    const [versionList, setVersionList] = useState([]);

    return (
        <div className="manage-version-page">
            <h1>This is manage version page</h1>
        </div>
    )
}

export default ManageVersion;